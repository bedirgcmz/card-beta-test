import CardForm from "@/components/CardForm";
import MainContent from "@/components/MainContent";

const Home = () => {
  return (
    <main className="">
      <MainContent />
    </main>
  );
};

export default Home;
